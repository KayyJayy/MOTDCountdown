name: ${project.artifactId}
main: ${project.parent.groupId}.${project.lowerCaseArtifactId}.${project.parent.artifactId}
version: ${project.version}-b${BUILD_NUMBER}
author: SystemUpdate <admin@systemupdate.io>